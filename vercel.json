{
   "version": 2,
   "builds": [
    {
      "src": "src/main.ts",
      "use": "@vercel/node",
      "config": {
        "outputDirectory": "dist"
      }
    }
  ],
   "routes": [
    { "src": "/(.*)", "dest": "/dist/$1" }
  ]
}

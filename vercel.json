{
   "builds": [
    {
      "src": "src/main.ts",
      "use": "@vercel/node",
      "config": {
        "outputDirectory": "dist"
      }
    }
  ],
   "rewrites":  [
       {"source": "/(.*)", "destination": "/"}
    ]
}
